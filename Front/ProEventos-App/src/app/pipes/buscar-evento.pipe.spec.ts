import { BuscarEventoPipe } from './buscar-evento.pipe';

describe('BuscarEventoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarEventoPipe();
    expect(pipe).toBeTruthy();
  });
});
